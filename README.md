# audio to text

## run guide

Create runtime and install packages

```bash
python3 -m venv .
source ./bin/activate
./bin/python -m pip install -U $(cat packages.txt)
```

Run

```bash
./bin/python main.py

```
